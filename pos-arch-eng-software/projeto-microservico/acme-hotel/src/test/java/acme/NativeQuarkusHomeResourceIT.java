package acme;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeQuarkusHomeResourceIT extends QuarkusHomeResourceTest {

    // Execute the same tests but in native mode.
}